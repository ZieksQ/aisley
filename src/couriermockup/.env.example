# Leave empty when using the Vite development proxy to the local Laravel API.
# Set this non-secret value to an API origin when the API is hosted elsewhere.
# Never put provider credentials in VITE_* variables; they are browser-visible.
VITE_API_URL=
