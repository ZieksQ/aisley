import { useEffect, useState } from 'react'
import { FaArrowsRotate } from 'react-icons/fa6'
import { ApiError, request } from '../lib/api'
type Dashboard = { hub: { id: string; name: string; address: { barangay: string; city_municipality: string; province: string; region: string } }; summary: null; orders: unknown[]; freshness: { generated_at: string; state: 'scaffold' } }
export function DashboardPage() { const [data, setData] = useState<Dashboard | null>(null); const [error, setError] = useState<string | null>(null); const [loading, setLoading] = useState(true); async function load() { setLoading(true); setError(null); try { setData(await request<Dashboard>('/api/v1/logistics/dashboard')) } catch (caught) { setError(caught instanceof ApiError ? caught.message : 'We could not load the dashboard.') } finally { setLoading(false) } } useEffect(() => { void load() }, []); if (loading) return <div className="p-6"><div className="h-24 animate-pulse rounded-lg border border-zinc-200 bg-white dark:border-white/10 dark:bg-[#18181b]" /></div>; return <div className="max-w-5xl p-5 sm:p-7"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-lg font-semibold">Dashboard</h2><p className="mt-1 text-sm text-zinc-600 dark:text-zinc-400">A secure starting point for your sole operational hub.</p></div><button className="inline-flex h-10 items-center gap-2 rounded-lg border border-zinc-300 px-3 text-sm font-medium hover:bg-zinc-100 dark:border-white/15 dark:hover:bg-white/10" onClick={() => void load()} type="button"><FaArrowsRotate /> Refresh</button></div>{error ? <section className="mt-6 rounded-lg border border-red-200 bg-red-50 p-4 text-sm text-red-800 dark:border-red-400/20 dark:bg-red-400/10 dark:text-red-300"><p>{error}</p><button className="mt-3 font-semibold underline" onClick={() => void load()} type="button">Try again</button></section> : null}{data ? <><section className="mt-6 rounded-lg border border-zinc-200 bg-white p-5 dark:border-white/10 dark:bg-[#18181b]"><h3 className="font-semibold">{data.hub.name}</h3><p className="mt-1 text-sm text-zinc-600 dark:text-zinc-400">{[data.hub.address.barangay, data.hub.address.city_municipality, data.hub.address.province, data.hub.address.region].filter(Boolean).join(', ')}</p></section><section className="mt-4 rounded-lg border border-zinc-200 bg-white p-5 dark:border-white/10 dark:bg-[#18181b]"><h3 className="font-semibold">Operational queue</h3><p className="mt-2 text-sm leading-6 text-zinc-600 dark:text-zinc-400">Parcel intake, waybills, sorting, transfer, dispatch, and Courier assignment will appear here once the approved shipment-state and hub-to-order relationships are available.</p><p className="mt-4 text-xs text-zinc-500 dark:text-zinc-500">Last checked {new Intl.DateTimeFormat(undefined, { dateStyle: 'medium', timeStyle: 'short' }).format(new Date(data.freshness.generated_at))}.</p></section></> : null}</div> }
