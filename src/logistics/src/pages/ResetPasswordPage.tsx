import { useState } from 'react'
import type { FormEvent } from 'react'
import { Link, useSearchParams } from 'react-router-dom'
import { AuthShell } from '../components/AuthShell'
import { Field } from '../components/Field'
import { ApiError, csrf, request } from '../lib/api'
export function ResetPasswordPage() { const [params] = useSearchParams(); const [email, setEmail] = useState(params.get('email') ?? ''); const [password, setPassword] = useState(''); const [confirmation, setConfirmation] = useState(''); const [notice, setNotice] = useState<string | null>(null); const [error, setError] = useState<string | null>(null); const [busy, setBusy] = useState(false); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setBusy(true); setError(null); try { await csrf(); const data = await request<{ message: string }>('/api/v1/logistics/auth/reset-password', { method: 'POST', body: JSON.stringify({ email, token: params.get('token'), password, password_confirmation: confirmation }) }); setNotice(data.message) } catch (caught) { setError(caught instanceof ApiError ? caught.message : 'We could not reach the API.') } finally { setBusy(false) } } return <AuthShell description="Choose a new password with upper- and lower-case letters and a number." footer={<Link className="font-semibold text-[#b0005d] hover:underline" to="/login">Back to sign in</Link>} title="Choose a new password"><form className="space-y-5" onSubmit={submit}>{notice ? <p className="text-sm text-emerald-700" role="status">{notice}</p> : null}{error ? <p className="text-sm text-red-700" role="alert">{error}</p> : null}<Field id="email" label="Email address" onChange={(event) => setEmail(event.target.value)} required type="email" value={email} /><Field id="password" label="New password" minLength={8} onChange={(event) => setPassword(event.target.value)} required type="password" value={password} /><Field id="confirmation" label="Confirm new password" minLength={8} onChange={(event) => setConfirmation(event.target.value)} required type="password" value={confirmation} /><button className="h-11 w-full rounded-lg bg-[#4C1268] text-sm font-semibold text-white disabled:opacity-60" disabled={busy} type="submit">{busy ? 'Saving…' : 'Reset password'}</button></form></AuthShell> }
