import { useState } from 'react'
import { FaArrowRightFromBracket, FaBars, FaGaugeHigh, FaTruckFast, FaXmark } from 'react-icons/fa6'
import { NavLink, Outlet, useNavigate } from 'react-router-dom'
import { useAuth } from '../auth/useAuth'
import { ThemeToggle } from '../components/ThemeToggle'
export function LogisticsLayout() { const { logistics, logout } = useAuth(); const navigate = useNavigate(); const [open, setOpen] = useState(false); const [signingOut, setSigningOut] = useState(false); async function signOut() { setSigningOut(true); try { await logout() } finally { navigate('/login', { replace: true }); setSigningOut(false) } } return <main className="min-h-screen bg-[#f7f7f8] text-zinc-950 dark:bg-[#101012] dark:text-white"><aside className={`${open ? 'flex' : 'hidden'} fixed inset-y-0 left-0 z-30 w-64 flex-col border-r border-zinc-200 bg-white px-4 py-5 dark:border-white/10 dark:bg-[#171719] lg:flex`}><div className="flex items-center justify-between"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-lg bg-[#4C1268] text-white"><FaTruckFast /></span><span><span className="block font-semibold">Aisley</span><span className="block text-xs text-zinc-500">Logistics workspace</span></span></div><button aria-label="Close navigation" className="lg:hidden" onClick={() => setOpen(false)} type="button"><FaXmark /></button></div><nav className="mt-8"><NavLink className="flex items-center gap-3 rounded-lg bg-purple-50 px-3 py-2.5 text-sm font-medium text-[#4C1268] dark:bg-white/10 dark:text-white" onClick={() => setOpen(false)} to="/dashboard"><FaGaugeHigh /> Dashboard</NavLink></nav><p className="mt-5 border-t border-zinc-200 px-3 pt-5 text-xs leading-5 text-zinc-500 dark:border-white/10">Operational features will be added as their shipment, dispatch, and Courier contracts are implemented.</p><div className="mt-auto border-t border-zinc-200 pt-4 dark:border-white/10"><p className="truncate px-2 text-sm font-medium">{logistics?.organization?.business_name ?? 'Logistics'}</p><p className="truncate px-2 text-xs text-zinc-500">{logistics?.email}</p><button className="mt-3 flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-sm text-zinc-600 hover:bg-zinc-100 disabled:opacity-50 dark:text-zinc-400 dark:hover:bg-white/10" disabled={signingOut} onClick={() => void signOut()} type="button"><FaArrowRightFromBracket />{signingOut ? 'Signing out…' : 'Sign out'}</button></div></aside>{open ? <button aria-label="Close navigation overlay" className="fixed inset-0 z-20 bg-black/55 lg:hidden" onClick={() => setOpen(false)} type="button" /> : null}<section className="min-h-screen lg:pl-64"><header className="flex h-16 items-center justify-between border-b border-zinc-200 bg-white px-4 dark:border-white/10 dark:bg-[#171719] sm:px-6"><button aria-label="Open navigation" className="lg:hidden" onClick={() => setOpen(true)} type="button"><FaBars /></button><span className="text-lg font-semibold">Logistics</span><ThemeToggle /></header><Outlet /></section></main> }
