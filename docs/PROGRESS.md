# Progress

Short, dated log of what's been implemented. Update this after every feature/change is completed — don't let it go stale.

Format:

```
## YYYY-MM-DD
- Feature/change short summary
```

---

## 2026-09-09

- Archived the previous 451-line progress log at `docs/logs/PROGRESS-2026-09-09.md` and added a 150-line progress-log archival rule to `AGENTS.md`.
- Added Seller pickup address-book CRUD with searchable PSGC fields and one default, per-Order pickup-address selection with immutable waybill snapshots, first-provider defaulting, and a searchable Logistics provider modal with hub details and recommendation/location tags.
- Changed solo and bulk Seller pickups to use one shared saved address per request, and added Geoapify geocoding with a click/drag Leaflet pin for pickup coordinates retained in immutable waybill snapshots.

## 2026-09-10

- Switched Seller registration and pickup-address PSGC selectors to the bundled `@aisley/psgc-address-data` loader and renamed the Seller Geoapify example variable to `GEOAPIFY_API_KEY`.
- Replaced the policy fixture's development-placeholder wording with generic Terms of Service, Privacy Policy, and Internal Platform Rules templates. Seeder behavior and schema are unchanged; existing policy versions remain protected.
- Fixed repeat Customer homepage requests under production-safe cache deserialization by caching only scalar advertisement, campaign, and category projections, rotating the affected cache keys, and covering guest plus Seller/Admin/Logistics session behavior; disabled prefetch for unimplemented storefront resource links to prevent background RSC 404 noise.
- Revised Courier Account Management into a standalone Phase 1 contract for own-profile read/update and password change, with exact planned `/api/v1/courier` routes, bearer-token and Flutter handoff rules, privacy/error/retry semantics, and explicit deferral of vehicle, license, payout, uploads, and shipment operations. No application behavior or migrations changed.
- Implemented Courier Account Management for the external Flutter client. Added protected `GET /api/v1/courier/account`, `PATCH /api/v1/courier/account/profile`, and throttled `PUT /api/v1/courier/account/password` endpoints with Courier role/status/Logistics-affiliation gates, server-derived ownership, allow-listed profile updates, transactional row locking, centralized password validation, complete bearer-token revocation after password changes, private no-store DTOs, and no Courier web UI. Focused Courier account coverage passes 8 tests/84 assertions (13 tests/125 assertions with the existing Courier suite); PHP formatting and diff checks pass. Vehicle, license, payout, profile-photo, email, and delivery operations remain deferred by the account contract.
- Added the root `pnpm dev:backend` runner, which starts the Laravel API, queue worker, and scheduler concurrently for backend-only development. The existing `pnpm dev` full workspace launcher remains unchanged.
- Revised the Customer Browse Seller Shops specification to `Implemented (Phase 1)`, documenting the existing Laravel/Next.js API and UI, visibility and scope guarantees, focused test coverage, and deferred enhancements. No runtime behavior or migrations changed.
- Revised the Customer Recently Viewed Items specification to `Implemented (Phase 1)`, documenting the existing API, guest storage/merge behavior, Product Detail recording, homepage and Account integrations, focused coverage, and deferred enhancements. No runtime behavior or migrations changed.
- Clarified the canonical-document hierarchy for Courier specifications and removed stale `docs/order-logistics-flow-decisions.md` prerequisite/reference links; the worksheet remains background history only. No runtime behavior or migrations changed.
- Clarified in Courier Account Management that profile-photo work must inherit the shared file-upload contract and define only Courier-specific behavior. No runtime behavior or migrations changed.

## 2026-09-10

- Synchronized the Courier Account Management implementation with the revised 230-line contract, corrected its implementation commit reference, and added regression coverage proving successive transactional profile writes return the latest full projection while preserving untouched fields. The focused account suite passes 9 tests/91 assertions; the complete Courier suite passes 14 tests/135 assertions. No new endpoint, migration, or Courier UI was added.
- Revised Courier Account Management to define the deferred profile-photo extension from the shared file-upload policy, including planned private endpoints, metadata migration, storage/cleanup rules, Flutter states, and unchecked photo acceptance criteria. No runtime behavior or migration changed.
- Implemented the Courier Account Management profile-photo extension: additive metadata migration, configured-disk/Azure-compatible multipart upload with UUID object keys and shared image validation, private owner-only stream, replacement/removal cleanup, rate limiting, DTO capability URL, and no Courier web UI. Focused Courier account tests pass 14 tests/167 assertions; the full Courier suite passes 19 tests/211 assertions. Updated `docs/schema.md` and the feature contract to mark all six account/photo routes implemented.
- Revised Customer Order Modification and Cancellation to clarify the implemented read-only boundary, the `placed`-until-Seller-processing window, unavailable mutation routes, immutable snapshot/versioning requirements, reservation-release boundary, idempotency, concurrency, notification, and Customer UI/test contracts. No application behavior or migration changed.
- Implemented Customer Order Modification and Cancellation Phase 1. Added Customer-scoped, idempotent `POST /api/v1/customer/orders/{order}/cancel` with transactional reservation release and immutable cancellation history, plus `PATCH /api/v1/customer/orders/{order}/modification` for saved delivery-address replacement using additive versioned Order snapshots. Updated capabilities, DTOs, schema documentation, and Customer Order detail confirmation/address-selection UI; variant, quantity, voucher, shipping, repricing, and post-pickup changes remain deferred. Focused Customer API coverage passes 16 tests/180 assertions; webapp TypeScript, lint, and Webpack production build pass.
- Fixed production PostgreSQL Order detail and checkout failures by replacing the UUID-aggregating `latestOfMany()` address relation with deterministic descending address-version ordering. Added regression coverage for selecting the current address without a one-of-many UUID aggregate; 16 focused Customer tests pass with 182 assertions.
- Revised Logistics Account Management into a 120-line implementation-ready contract aligned with the existing one-account/one-organization/one-hub foundation. It records that account API/UI work is not implemented, defines protected personal/organization boundaries, blocks unapproved hub relocation, keeps subscription and approval separate, and specifies planned endpoints, security, SPA states, tests, and open decisions. No runtime behavior or migration changed.
- Implemented Logistics Account Management Phase 1: protected private account projection, allow-listed transactional personal and organization/sole-hub-label updates, fail-closed relationship loading, current-password-protected password changes with token revocation and throttling, no-store responses, and application audit context. Added the responsive dark-mode Account Settings page with Personal profile, Organization, read-only Operational hub, and Security sections, plus navigation and session refresh integration. Focused Logistics API coverage passes 8 account tests (87 assertions; 11 tests/105 assertions including authentication), and the Logistics TypeScript, lint, and Vite production-build checks pass. Address relocation, email/MFA, profile photos, staff, and Courier web UI remain deferred.
- Specified a deferred Logistics organization business-logo extension in Account Management. It inherits the shared file-upload policy and defines organization ownership, Azure-compatible storage metadata, private delivery, replacement/removal cleanup, and SPA upload states; personal profile photos remain out of scope. No runtime behavior or migration changed.
- Implemented the Logistics personal profile-photo extension: additive profile metadata migration, configured-disk/Azure-compatible multipart upload with UUID keys and strict JPEG/PNG/WebP validation below 10 MiB, private owner-only delivery, replacement/removal cleanup, throttling, safe DTO capability path, and Account Settings initials/photo/progress/remove UI. Focused Logistics account coverage now includes upload, replacement, authorization, validation, format/boundary, cleanup, and rate-limit cases; the organization business-logo extension remains deferred.
- Expanded the configured role seeders with 20 additional Customers, 5 Sellers, and 5 Logistics accounts using each role's `.env` password, deterministic configurable email prefixes/domains, complete Seller pickup addresses, and complete Logistics organization/hub foundations. Added idempotency coverage and documented the generic-account controls in `src/api/.env.example`.
- Revised Customer Product Q&A into a concise deferred contract aligned with the current Customer/Seller domains. It clarifies canonical Customer terminology, Product visibility and ownership, planned question/answer endpoints, notification and idempotency boundaries, safe public DTOs, UI states, and the absence of current Q&A implementation. No runtime behavior or migration changed.
- Implemented Customer Product Q&A Phase 1: additive UUID `product_qas` schema/model, public visibility-scoped paginated reads, active-Customer plain-text questions, owning-Seller one-answer API, actor-scoped idempotency, deterministic after-commit notifications, Customer/Seller notification projections, and the accessible Product Detail Q&A section with sign-in, validation, pagination, unavailable, and retry states. Focused Q&A/Product Detail coverage passes 10 tests/92 assertions; webapp lint and TypeScript checks pass. Seller answer-management UI remains deferred until its Seller specification exists.
- Added `src/couriermockup`, a plain React/Vite development harness for the implemented Courier login, bearer-token session restore, account/profile/password, logout, and dashboard-scaffold API routes. Added local mockup rules enforcing `Authorization: Bearer` with `credentials: omit`, an optional root dev script, and no invented operational Courier UI; TypeScript build and oxlint pass.
- Revised the Courier Pick Up Order specification to cover selected-Courier receipt after bulk scheduling, schedule/pickup/order details, QR or manual Order ID verification, explicit `picked_up_from_seller` confirmation, bounded Geoapify Matrix route manifests with address-coordinate fallback, GeoJSON map visuals, free-tier safeguards, and offline QR dependency options. Logistics parcel receipt remains explicitly N/A; no application behavior or packages changed.
- Implemented Courier Pick Up Order Phase 1 in Laravel and the development-only `src/couriermockup`: tenant-scoped task receipt/acceptance, lazy-loaded local ZXing QR capture, manual Order-reference fallback, explicit idempotent `picked_up_from_seller` confirmation, immutable custody history, Inventory reservation fulfillment, private responses, and truthful mobile-oriented error states. Route manifests/maps and Logistics receipt remain deferred; focused API tests pass 6 tests/128 assertions, the wider Courier/pickup suite passes 25 tests/339 assertions, and the mockup build/lint pass.
- Implemented Courier Pick Up Order Phase 2 route manifests: schedule-revision persistence and queued calculation, exact/address-default coordinate resolution, bounded Geoapify full Matrix calls with daily credit guard, deterministic nearest-next-stop ordering and hub return, same-address parcel grouping, sanitized GeoJSON, Courier-scoped manifest and credential-hiding cached tile proxy routes, and a lazy MapLibre map plus ordered fallback list in the development mockup. The Courier/pickup suite passes 27 tests/375 assertions and the mockup build/lint pass; Logistics dashboard mapping, road geometry, and Flutter remain deferred.
- Set the initial Logistics operational hub seed coordinate to Barangay Poblacion, Makati City (`14.565681, 121.032077`) with environment overrides and regression coverage. Source checked against the published Poblacion coordinate references.
- Added road-following Geoapify Routing geometry to Courier pickup manifests in the server-authoritative Logistics hub → numbered pickups → Logistics hub order, with bounded waypoint chunking, separate quota/timeout controls, a labelled straight-line fallback, and a visible Logistics start/end map marker. The Courier/Logistics pickup suite passes 27 tests/382 assertions; courier mockup build and lint pass.
- Fixed existing Courier pickup manifests disappearing after the route-line rollout by rendering any GeoJSON `LineString`, versioning the road-geometry fingerprint, and queuing legacy ready manifests for background regeneration.
- Hardened Courier route rendering with a dedicated line-only MapLibre source, a client stop-sequence fallback when stored line geometry is absent, and a high-contrast cased route layer that remains visible across Geoapify tile colors.

## 2026-09-11

- Hardened Courier pickup route-map loading with bounded pending-manifest polling, truthful MapLibre/tile error fallback, retry-state reset, invalid-line protection, and Courier mockup CORS defaults; focused route coverage passes 2 tests/48 assertions and the mockup build/lint pass.
- Aligned Seller Geoapify pinning with the configured `GEOAPIFY_API_KEY` variable, explicitly exposed that browser-key prefix through Vite, and updated the map policy reference; Seller type-check/lint and seven order-action tests pass.
- Enabled one Courier pickup schedule to combine solo or bulk handoffs from multiple Sellers while retaining tenant isolation, conflict checks, per-parcel tasks, Seller-specific notifications, route-stop grouping, and the 30-parcel cap. Added cross-Seller batch selection and confirmation to the Logistics Pickups queue; focused API coverage passes 9 tests/197 assertions and the Logistics build/lint pass.
- Redesigned Logistics Pickups around schedules: added a tenant-scoped schedule summary API with Courier, linked Pickup IDs, status, pickup window, total parcels, and remaining parcels; moved assignment into a **Create new schedule** workspace with pending parcels sorted by Shop and oldest request, bulk/individual selection, and the enforced 30-parcel cap. Removed the Courier mockup's local plain-UI restriction while preserving its Flutter-production and API-security boundaries. Focused API coverage passes 9 tests/210 assertions and the Logistics build/lint pass.
- Kept the Courier mockup's bulk route manifest and MapLibre map mounted when selecting another Seller parcel within the same pickup schedule, so only the parcel pickup details, waybill, QR workflow, and Order reference update; changing schedules still loads that schedule's route.
- Fixed Courier bulk pickup confirmation so scanned waybills and manually entered Order references resolve to the matching accepted parcel within the open schedule instead of failing against the previously selected row. Explicit confirmation now atomically records `picked_up_from_seller`, advances the Order from `ready_for_pickup` to `picked_up`, fulfills reserved Inventory, and appends Order status history; wrong, foreign-schedule, or unaccepted matches remain non-mutating.
- Revised Seller Vacation Mode to an implemented Phase 1 contract covering the immediate own-Shop toggle/message in Account Management, public visibility and Cart/Checkout guards, existing Order/Inventory boundaries, current UI behavior, and deferred scheduling/presentation variants. No runtime behavior or migration changed.
- Created the Seller Product Q&A Answer Management specification aligned with the implemented owning-Seller answer API, Customer Q&A contract, Shop-scoped authorization, idempotency, after-commit notifications, safe DTOs, and deferred Seller UI. No runtime behavior or migration changed.
- Implemented Seller Product Q&A Phase 2: Shop-scoped paginated queue/detail APIs with answer-state and Product filters, safe Seller projections, historical archived-Product read-only visibility, notification deep links, and accessible dark-mode React queue/detail pages with retry-safe idempotent answers. Focused Customer/Seller Q&A coverage passes 9 tests/58 assertions; Seller TypeScript, lint, and production Vite build pass. Answer editing/history and moderation remain deferred.
- Revised Admin Manage Platform Settings to mark announcement/policy CRUD and public policy history implemented while explicitly separating the deferred policy-consent matrix, acceptance API/UI, and authentication/session/protected-action enforcement. No application behavior or migration changed.
- Added the cross-role Policy Viewing and Consent specification, separating implemented public Terms/Privacy reads from the deferred consent matrix, acceptance API, user-facing pages, and auth/session enforcement. No application behavior or migration changed.
- Added `docs/features/shared/shipment-fulfillment/spec.md` as an isolated, non-authoritative reference outline for future shipment/fulfillment work. It does not modify or supersede canonical documents or authorize operational implementation.
- Implemented shared policy viewing Phase 1: cacheable public Terms/Privacy current, history, and exact-version API reads with focused visibility/cache tests; server-rendered webapp policy pages with safe plain-text rendering, metadata, retry/not-found states, and history navigation; storefront/auth/dashboard policy links for Customer, Seller, Admin, and Logistics. Consent matrix, status/acceptance endpoints, and auth enforcement remain intentionally deferred until policy decisions are approved.
- Revised the Policy Viewing and Consent contract so Terms of Service and Privacy Policy are single platform-wide version streams shared by every account role, with no role- or tenant-specific public variants. Consent timing and enforcement remain deferred; no runtime behavior or migration changed.
- Implemented shared policy consent Phase 2 from the revised contract: private, no-store status and exact-current-version acceptance APIs for active Customer, Seller, Admin, Logistics, and approved Courier accounts; idempotent immutable acceptance with stale-version conflicts and rate limits; Customer, Seller, Admin, and Logistics consent screens using the same platform-wide documents; and focused API coverage. A global auth/session/protected-action gate remains deferred because the revised contract still leaves its exact blocking trigger open.
- Added the idempotent `PlatformPolicySeeder` and editable JSON fixture at `src/api/database/seeders/data/platform-policies.json` for development Terms of Service, Privacy Policy, and Internal Platform Rules placeholders. It runs from `DatabaseSeeder`, creates initial published versions only when a policy has no versions, preserves existing policy history on reruns, and skips placeholder content in production. Focused seeder and policy API tests pass 15 tests/171 assertions.
- Expanded the policy JSON fixture with generic Terms of Service, Privacy Policy, and Internal Platform Rules version 1 and version 2 entries. The seeder now creates all fixture versions on an empty policy stream, marks earlier versions superseded, points to the latest version, rejects duplicate type/version entries, and preserves existing streams on reruns.
- Fixed policy current-version cache deserialization failures by rotating the policy cache key and caching the resolved scalar DTO instead of a serialized `PlatformPolicyVersion` model. Added regression coverage for the array projection.

## 2026-09-11

- Revised the shared Shipment and Fulfillment document into an active cross-document decision and revision guide. It replaces ambiguous “approved elsewhere” language, directs synchronized updates to canonical requirements/workspace/schema/domain/spec files, and records the one-hub, one-Order/Parcel task, independent first-/final-mile assignment, status, inventory, scan/evidence authority, rejected-versus-stale task handling, deferred returns/refunds/partial fulfillment, and provider-neutral Courier distance/ETA decisions. Canonical-document propagation remains pending; no runtime behavior or migration changed.
- Propagated the accepted Shipment/Fulfillment decisions into `docs/requirements.md`, `docs/workspace.md`, `docs/schema.md`, `docs/domains/Logistics.md`, and `docs/domains/Courier.md`: Logistics authority over Courier-submitted QR/evidence, task rejection/re-offer and informational staleness, provider-neutral Courier distance/ETA, and separate task-level outcomes. Physical Shipment/Parcel/Scan/final-mile/proof-of-delivery implementation remains deferred; no migrations or runtime code changed.
- Revised the Logistics Dashboard, Deploy Rider, and Update Status specifications from the shared Shipment/Fulfillment contract. They now distinguish the implemented dashboard scaffold from unavailable operational APIs, define rejected-task visibility/re-offer and informational staleness, require provider-neutral distance/ETA, and make Logistics the authoritative recorder of Courier-submitted scans/evidence. No runtime code or migrations changed.
- Revised the Courier Dashboard, Accept Delivery Requests, Pick Up Order, Proof of Delivery, and Deliver Order specifications from the shared Shipment/Fulfillment contract. They now define task visibility, rejected/re-offered and stale states, provider-neutral distance/ETA, Courier QR/evidence submission to Logistics, Logistics validation/recording authority, and explicit deferred operational endpoints without Mapbox assumptions. No runtime code or migrations changed.
- Revised the shared Logistics Pickups and Seller-Created Waybill specifications from the Shipment/Fulfillment contract. Courier acceptance is now explicit before physical handoff, rejection is a task-level outcome that Logistics may re-offer on the same task, and unfinished work may be informationally stale without automatic reassignment. Waybill QR scans are documented as Courier submissions to Logistics for validation/authoritative recording, separate from access events and custody transitions. No runtime code or migrations changed.
- Revised the shared Shipment and Fulfillment guide to restore an implementation-readiness worksheet. It now provides explicit questions and answer/owner fields for Shipment/Parcel identity, task legs and state vocabulary, Logistics scope, Courier offers, actor/audit presentation, idempotency, scan/evidence notification, append-only history, transition matrices, cancellation boundaries, migration order, transition-service ownership, endpoint ownership, and verification. No runtime behavior or migration changed.
- Revised the shared Shipment and Fulfillment guide with a reconciliation register for Seller-owned Logistics selection, corrected high-level status mapping, exact endpoint-spec paths, implemented-versus-deferred summaries, schema-ledger synchronization, policy-consent wording, and legacy-reference cleanup. No runtime behavior or migration changed.
- Reconciled canonical documentation with Seller-owned Logistics selection, first-mile `picked_up` semantics, implemented pickup/waybill/Inventory/Q&A boundaries, the complete repository migration ledger, and implemented policy consent. Added explicit current-versus-future Logistics evidence-validation compatibility, accepted operational record/transition summaries, and separate decision/test-execution gates. Historical logs and unrevised draft contracts remain historical; no runtime code or migrations changed.
- Reconciled the planned physical fulfillment contract: same-task rejection/re-offer, deferred automatic expiry/internal transfer, required hub sorting, dependency-ordered additive schema/service rollout, and a legacy first-mile bridge preserving confirmations and inventory effects without replay. Revised Courier Complete Delivery/Delivery History and Seller Delivery Confirmation around Logistics-validated final delivery, and synchronized affected domain/Logistics references. Documentation only; no backend, migration, or database-test execution was performed. Physical activation and verification gates remain pending.
- Enforced shared Terms of Service and Privacy Policy consent before protected Customer, Seller, Admin, Logistics, and Courier API actions. Added the machine-readable `403 POLICY_CONSENT_REQUIRED` response with required version/read/accept/status links, kept authentication bootstrap/logout/consent routes reachable, added role SPA route guards for Customer/Seller/Admin/Logistics, and covered denial plus post-acceptance access in focused API tests. Updated the shared policy, auth, schema, Admin settings, and fulfillment documentation to record the implemented gate.
