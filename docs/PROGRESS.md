# Progress

Short, dated log of what's been implemented. Update this after every feature/change is completed — don't let it go stale.

Format:

`
## YYYY-MM-DD
- Feature/change short summary
`

---

## 2026-09-15

- Revised the Logistics-owned Courier vehicle registry and related registration, approval, dispatch, domain, schema, and workflow documentation for exactly one vehicle per Courier with required type/plate and private OR/CR. Deferred maintenance, vehicle history, and capacity values/units/matching; documented existing one-image upload and missing database uniqueness without claiming implementation. No runtime, migration, seed, or Flutter changes.

- Defined approved Courier vehicle edits for type/plate/optional make/model and separate, independently replaceable OR/CR uploads without Logistics reapproval. Added planned private Courier/Logistics API contracts, revision/idempotency and migration boundaries, preserved original approval evidence, and specified durable associated-Logistics change notifications. Synchronized related auth/account/approval/notification and canonical docs; new acceptance remains unchecked. Documentation only; no backend, migration, database, or Flutter changes.

- Implemented Courier Vehicle Fleet Management Phase 1 in Laravel: additive one-vehicle-per-Courier uniqueness with duplicate preflight, optimistic revision/idempotent type/plate/make/model edits, independently replaceable private OR/CR document metadata and streams, strict shared image validation, scoped Logistics registry reads, cleanup of unreferenced replaced files, and deterministic after-commit `logistics-courier.vehicle-updated` notifications. Added focused API coverage (10 tests/125 assertions including Courier approval), synchronized canonical docs, and kept Courier mobile UI/registration-field rollout separate. PostgreSQL migration verification remains pending while the local Docker daemon is unavailable.

## 2026-09-16

- Archived the previous 148-line progress log at `docs/logs/PROGRESS-2026-09-16.md` and added thin Code 128 tracking-ID waybills, tenant-scoped postal-code sort plans, server-authoritative automatic sorting with exception fallback, responsive Sort plan UI, and synchronized role contracts. Logistics/Courier coverage passes; the unrelated CustomerRecentlyViewed 404 failures remain documented as out of scope.

- Corrected stale Courier Account Management and Logistics vehicle-route implementation wording, refreshed schema synchronization metadata, and narrowed deferred fleet scope to advanced extensions. Re-ran `CourierVehicleFleetManagementTest` and `CourierApprovalTest` against in-memory SQLite: 10 tests/128 assertions pass, superseding the 125-assertion count in the September 15 vehicle entry. Historical entries remain intact; PostgreSQL verification, external Flutter rollout, and separate-document registration rollout are not certified by this check. Documentation only; no application code changed.

## 2026-09-17

- Revised Logistics Vehicle Fleet Management and Notifications specs for a protected, read-only Courier vehicle detail page reached from the existing vehicle-update notification destination, with current OR/CR private previews and safe unavailable states. The existing vehicle/notification APIs are distinguished from the missing React route; a browsable fleet list is separately gated on a bounded list API. Corrected stale notification producer and endpoint status wording. Documentation only; no Logistics UI or backend behavior changed.

- Implemented the protected Logistics Courier vehicle detail UI at `/couriers/:courierId/vehicle` and connected vehicle-update notification details with an **Open Courier vehicle** action. The page refreshes current scoped vehicle fields and loads OR/CR privately on demand, with missing/unavailable, offline, access, and consent states plus preview cleanup. Updated both feature specs; no fleet-list endpoint or sidebar entry was added. Logistics TypeScript, oxlint, and Vite production build pass; focused vehicle/notification API coverage passes 9 tests/85 assertions. Browser interaction verification remains pending.

- Added the Logistics **Vehicles** sidebar entry and responsive, searchable, paginated vehicle list so current Courier vehicles can be opened without a notification. A private no-store `/api/v1/logistics/vehicles` endpoint returns only active Couriers with approved affiliations to the current organization and sole hub, with safe summaries and no document bytes/paths; the existing detail remains read-only. Updated fleet/notification/domain docs. Focused fleet and notification API coverage passes 11 tests/118 assertions; Logistics TypeScript, oxlint, and Vite build pass. Browser interaction verification remains pending.

- Fixed all three Logistics camera scanning flows (Receive at hub, Sorting, and Parcel search) to use a shared 1D Code 128-only reader that ignores QR, densely samples thin bars, and prefers a higher-resolution rear camera. Preserved Code 128 lane-label scanning and manual/offline handling; corrected camera previews/guidance and affected specs. Three scanner regression tests pass using the actual PHP label encoder (thin tracking bars at three module widths, lane labels, and QR rejection); Logistics TypeScript, oxlint, and production build pass. Physical camera/printed-waybill verification remains pending. Applied the uncodixfy skill to keep UI guidance changes within the existing dashboard layout.

## 2026-09-18

- Added an implementation-ready Hub-to-Hub Transfer Routing specification covering service-area destination resolution, directed hub graph connections, Geoapify Matrix/Routing measurements, Dijkstra route snapshots, route-hop custody, hub-targeted sort lanes, tenant boundaries, future transfer-penalty/operating-cost weights, additive schema changes, API boundaries, acceptance criteria, rollout, and observability. Documentation only; no runtime, migration, database, or frontend changes.

- Implemented Hub-to-Hub Routing APIs behind disabled-by-default `HUB_ROUTING_ENABLED`: additive string-backed route/hop/service-area/connection schema and current custody backfill, Admin permission/revision/audit configuration, Geoapify directed matrix metrics with coordinate caches/shared quota, deterministic duration/distance Dijkstra snapshots at waybill creation, locked lazy Shipment attachment, tenant-safe route reads, idempotent transfer departure/arrival events, next-hop sorting lanes, exception holds, and destination-only final-mile dispatch/Courier/notification scope. Preserved immutable origin, first-mile inventory, local/legacy compatibility, and Order status boundaries; synchronized affected API specs. Focused SQLite coverage passes 22 tests/313 assertions with one PostgreSQL-only concurrency skip; PostgreSQL passes all 23 tests/338 assertions including independent concurrent departure/arrival workers and migration rollback/backfill. Broader SQLite regressions pass 92 tests/1,440 assertions; later targeted fulfillment/routing passes 31 tests/633 assertions plus the same skip. Existing PostgreSQL fulfillment/waybill/notification/Customer status regressions pass 33 tests/708 assertions (older SQLite-only migration test excluded there). Pint passes. Disposable PostgreSQL test database only; no app-data migration or frontend/Flutter changes. Network setup, hold recovery policy/action, bilateral consent, and physical linehaul evidence/automation remain rollout boundaries.

- Implemented Hub-to-Hub Routing as a compact Logistics Sorting / Sort plan extension using the uncodixfy skill: allowed next-hub lane mappings beside postal-code routing, safe next-hop/hold reconciliation context, online parcel-route lookup and physical departure/arrival confirmations with Shipment/hop revisions and matching-request retries after uncertain network failures. Kept small help/refresh icons at the top right on mobile, reduced page spacing, retained dark mode and responsive forms, and preserved the offline sorting outbox. Added tenant-safe active outgoing hub summaries to the existing plan API and a focused isolation regression; routing tests pass 21 tests/291 assertions and fulfillment/sorting/final-mile regressions pass 9 tests/323 assertions. Logistics TypeScript, oxlint, Vite production build, and PHP formatting pass. Updated frontend acceptance criteria; actual browser/device visual interactions and physical barcode/handoff verification remain pending because this environment has no browser. Routing still defaults off; Admin network UI, hold recovery, consent policy, and physical linehaul automation remain separate rollout boundaries.

- Added the production-guarded `LuzonLogisticsSeeder` development fixture: one active, email-verified Logistics account and sole pinned hub for each of the eight Luzon regions (NCR, CAR, Regions I, II, III, IV-A, IV-B, and V), deterministic `logistics.luzon01@example.com`–`08` emails, and shared `logistics123` password. Provinces follow regional logistics-center references while addresses remain explicitly representative demo locations. Added idempotence, coordinate, province, and production-skip coverage; focused seeder tests pass 8 tests/75 assertions and registration-management regressions pass 12 tests/63 assertions.

- Revised hub transfers as **Linehaul** using DHL/FedEx consolidation research: enabled-by-default audited Admin Feature controls switch, tenant-owned postal coverage/outgoing connection setup without extra platform approval, explicit operator-recorded road measurements alongside Geoapify, and Dijkstra travel-plus-handling scoring (initial 30-minute allowance per hop). Added immutable same-next-hub manifests with atomic departure/receipt, matching-request retries, tenant-safe ready groups/history, and prevention of individual manifest-member transfers. Receipts remain available after feature/connection shutdown; legacy standalone transfer compatibility remains. Simplified Sort plan with create/edit native dialogs, selected-plan actions, confirmed revision-checked deletion, and clearer destination-to-lane guidance following uncodixfy. Updated affected specs; applied the additive migration to local PostgreSQL. SQLite API regressions pass 54 tests/1,009 assertions with one PostgreSQL concurrency skip; Admin/Logistics lint and production builds pass, and PHP formatting passes. Browser/device handoff checks and new-manifest PostgreSQL concurrency testing remain pending; existing held routes are not retroactively rerouted.

## 2026-09-19

- Fixed the empty Sort plan hub selector: list other active Logistics hubs without requiring preconfigured connections, and atomically create/enable only the authenticated hub's outgoing connection when saving a hub mapping, preserving existing road measurements. No extra Admin permission/approval; the platform linehaul toggle remains authoritative for new routing/departures. Added the requested sidebar Beta tag and contextual mapping guidance using uncodixfy. Updated Sorting/Linehaul specs; routing and Linehaul SQLite coverage passes 27 tests/391 assertions, PHP formatting passes, and Logistics lint/production build pass. Browser/device interaction verification remains pending. No migration required.

- Automated Linehaul manifest membership from the active Sort plan and route: the API now returns ready groups by next hub, rechecks eligible sorted parcels server-side, and accepts only `next_hub_id` from the Sorting UI. Removed manual parcel-reference entry while keeping physical departure confirmation and idempotent retry safety. Updated the Linehaul contract and focused coverage passes 29 tests/400 assertions (including 6 Linehaul tests/103 assertions); Logistics lint and production build pass.

- Separated Linehaul into its own protected Logistics page with the requested sidebar Beta tag, removing transfer/configuration controls from Sorting. Added receiver-scoped acceptance/decline/disconnection, sender withdrawal, revision checks, and additive boolean consent migration; Sort plan now maps only accepted active next hubs without enabling connections. Preserved automatic route-driven lanes, atomic manifests/receipts, and enabled retries after uncertain departures. Reduced Geoapify calls by pruning destination-irrelevant branches and accounted for documented long-distance credit surcharges alongside 24-hour caches/local Dijkstra. Vehicle/Courier assignment and capacity-based automatic disconnection remain deferred. SQLite consent coverage passes 9 tests/131 assertions and sorting/final-mile regressions pass 9 tests/323 assertions; PostgreSQL routing/consent/migration/configuration/concurrent transfers pass. The existing SQLite-only PRAGMA migration test passes on SQLite and is excluded from PostgreSQL verification. Logistics build/lint, Pint, and diff checks pass; applied the consent migration to local PostgreSQL. Existing unilateral links require receiver acceptance; committed held routes are not rerouted. Browser/device and physical handoff checks remain pending.

- Replaced the Linehaul hub-selector connection form with a searchable, paginated active Logistics partner directory and per-partner Connect/withdraw/disconnect actions, receiver acceptance, a Sort plan link, and separate optional road measurements/coverage settings using uncodixfy. Simple connection actions preserve stored metrics. Automatic sorting now retries held origin routes with no committed hops after network setup while preserving route identity, custody, planned hops, and scan replay. Documented coverage-based routing and why missing local lane mappings must not cause arbitrary forwarding. Focused SQLite connection/routing/Dijkstra coverage passes 35 tests/471 assertions and final-mile regressions pass 9 tests/323 assertions; Logistics lint/build, Pint, and diff checks pass. No migration required; PostgreSQL concurrency and browser/physical handoff checks for this revision remain pending.

- Fixed Logistics camera startup/lifecycle in Receive at hub, Sorting, and Parcel search using uncodixfy: shared stream ownership releases tracks during pending startup, stable scan handlers avoid camera restarts on page updates, and muted inline autoplay uses explicit playback with bounded wait. Added basic-constraints fallback and actionable HTTPS/browser/permission/missing-camera/busy-camera/playback errors. Nine scanner regressions pass, including thin Code 128/lane decoding with the actual PHP waybill encoder, QR rejection, and startup cancellation; Logistics lint, TypeScript, production build, and diff checks pass. Updated waybill/sorting specs. Physical device/printed-PDF verification remains pending; HTTP LAN addresses require HTTPS for browser camera access.

- Followed up the reported Brave/laptop camera opening then silently closing: replaced ZXing’s stream-finalizing frame loop with bounded polling that waits for usable video dimensions/current data, retries temporary `InvalidStateError` and unreadable frames without releasing the camera, and reports fatal decoder failures through existing page notices. Startup retries interrupted playback at canplay and cleans up listeners on completion/cancellation. Confirmed installed ZXing Code 128 support using the actual waybill encoder; 11 scanner regressions pass, including temporary-frame recovery, interrupted playback, and visible fatal-error reporting. Logistics lint, TypeScript, build, and diff checks pass; physical Brave camera/printed-label verification remains pending. Updated matching scanner specifications.

- Reproduced the remaining scanner failure in installed `@zxing/browser` 0.2.1: `TRY_HARDER` rotates ordinary blank frames through an uninitialized temporary canvas and throws “Could not create a Canvas element.” Replaced canvas-backed luminance conversion with direct RGBA grayscale conversion and buffer rotation while preserving dense Code 128-only decoding; added local startup/decode exception diagnostics without decoded-payload logging. Fourteen regressions pass, now covering actual blank-frame conversion, camera retention without a mocked decoder, and horizontal/rotated bars from the PHP waybill encoder. Logistics lint, TypeScript, production build, and diff checks pass; updated scanner specs. Prior bitmap/mocked-frame tests did not cover the faulty dependency path; physical Brave camera/printed-label verification remains pending. The reported 113ms timeout violation is a scan-duration warning, not evidence of the thrown exception.

## 2026-09-20

- Revised the Linehaul routing specification for a connection-only partner page with modal request decisions and rejection reasons, separate approval and live connection states, bidirectional transfer eligibility, Sort plan supported-postal-code management and constrained lane selectors, and multi-hub routing continuity. Documented migration, API, UI, and verification boundaries while preserving existing custody and manifest behavior. Documentation only; no runtime or schema change.

- Redesigned the Logistics Linehaul and Sort plan frontend with uncodixfy: Linehaul now presents a compact partner directory, pending-request modal, and help; Sorting owns grouped departure and manifest receipt controls; Sort plan owns supported postal codes and a viewport-bounded plan workspace. The workspace shows selected plan information, mappings ordered by physical lane number, and physical lanes on the left, with a selectable plan list and Save/Activate/Delete controls on the right; narrow screens put selection first and actions in a bottom row. New plans start inactive. Updated affected specifications. Logistics lint and production build pass; browser/device visual checks remain pending. Current API still lacks separate approval/live states, rejection reasons, bidirectional partnership semantics, and server-side supported-code enforcement, so the remaining 2026-09-20 acceptance criteria are still open.

- Refined Sort plan with uncodixfy: the main page now shows only the active plan's ordered destination lanes, plus physical lanes and supported postal codes; each list has independent eight-row pagination. Other plans remain in the workspace dialog, whose Save/Activate/Delete actions now share an aligned grid. Added an additive per-hub postal uniqueness migration so multiple active hubs may support one code, removed global uniqueness checks in Logistics/Admin writes, and extended route calculation to choose the lowest-cost reachable supporting hub (local first; connected active graph, driving-plus-handling time, road distance, hops, stable IDs). Updated routing/Sorting/schema contracts. Focused SQLite API suite passes 38 tests/505 assertions; Logistics lint/build, Pint, and diff checks pass. PostgreSQL migration and browser/device verification remain pending; the separate approval/live connection and server-side plan-postal validation work from the current specification remains open.

## 2026-09-20

- Added a protected Logistics 404 page for unmatched routes with accessible messaging, dark-mode styling, a page title, and a Return to dashboard action. Logistics lint and production build pass.

## 2026-09-20

- Extended the production-guarded `LuzonLogisticsSeeder` with five active, approved, vehicle-equipped Courier fixtures per each of the eight regional Logistics organizations (40 total), deterministic development credentials/emails/plates, Courier profiles, addresses, hub affiliations, and varied vehicle type/make/model data. Added idempotence and production-skip assertions; focused seeder coverage passes 2 tests/36 assertions and Pint passes.

- Expanded the development-only Courier API mockup with the implemented final-mile offer, hub pickup evidence, movement, proof, completion-intent, and history routes; added QR/manual identifiers, revision/idempotency requests, authoritative refreshes, and pending Logistics-validation states. Added public registration with PSGC address selection and current combined vehicle-registration evidence, the generic recovery entry point, private profile photo management, and sole-vehicle edit/separate OR/CR replacement/preview. Updated matching API-client specs and mockup README. Courier mockup TypeScript/Vite build and oxlint pass; browser/device and live Laravel interaction checks remain pending. The mockup remains a bearer-token development harness, not the external Flutter production client.

## 2026-09-20

- Changed destination-hub final-mile work to atomic Courier acceptance of the existing 1–15 parcel dispatch schedule while preserving per-parcel custody. Replaced delivery QR/reference proof with private JPEG/PNG/WebP photo POD, a separate Delivered intent, Logistics photo preview/confirmation or reasoned rejection, and retryable failed attempts that leave `out_for_delivery`; an attempt after a photo requires fresh POD. Added scoped Geoapify Matrix stop ordering, Routing road `LineString`, and MapLibre tile/route display in the development Courier mockup, excluding linehaul. Synchronized Courier/Logistics/shared specs, domain, requirements, schema, map policy, and mockup README. Focused SQLite final-mile coverage passes 10 tests/389 assertions; Courier/Logistics lint and TypeScript/Vite builds pass; Pint and diff checks pass. PostgreSQL migration and physical mobile/map/photo review remain unverified.

## 2026-09-21

- Followed up failed physical Logistics scans of photographed waybill barcodes. Reproduced Code 128 decode failure from the complete A6 PDF at 96 DPI while 144/192 DPI decode; cropping and digital enlargement could not recover the lost bar detail. Increased the printed barcode's width/height and white side margins, added Code 128-first waybill QR fallback to the shared Receiving/Sorting/Parcel search camera while rejecting unrelated QR codes, and clarified close-framing guidance. The QR still resolves through existing tenant-scoped APIs. Focused scanner tests pass 16/16; complete-PDF decode passes at 144/192 DPI; Logistics lint and production build pass. On-device retesting with the photographed/printed label remains required.

## 2026-09-21

- Corrected final-mile Courier map presentation: authenticated Geoapify `osm-bright` tiles, visible Logistics start and numbered delivery markers, and a cased MapLibre GeoJSON `LineString` drawn when the style loads. Known coordinates and a labelled straight-line fallback remain visible when Matrix/Routing fails; unpinned stops are not fabricated. Final-mile hub handoff now uses the accepted task and revision without parcel identifier input, while Logistics still validates custody; first-mile scans remain separate. Exposed assigned parcel merchandise subtotal/currency to the Courier, opened camera capture from the POD action, and kept private photo plus Delivered intent for Logistics review. Updated affected API/client specifications and mockup guidance. Focused API, Courier build/lint, and PHP formatting checks pass; live Geoapify tiles and physical device camera/map behavior remain unverified.

- Fixed Seller pickup failures caused by Geoapify hub-metric cache keys exceeding PostgreSQL's 255-character cache-key column by hashing the full metric identity into a bounded key. Added Hub Routing regression coverage; focused suite passes 23 tests/305 assertions and Pint passes. No migration required.
- Revised the ignored `docs/cabigan/future-plan.md` against current canonical docs, API routes, Logistics pages, and the copied Flutter progress snapshot. Removed stale claims that hub operations, vehicle UI, and both-leg Flutter integration still need to be built; prioritized pending Linehaul partnership/postal validation, end-to-end verification, and genuinely unimplemented standalone work. Planning documentation only; no runtime, database, migration, seed, or Flutter code changed.

- Added the Courier Notifications target spec under `docs/features/courier/notification/`: existing pickup-schedule database alerts are separated from unavailable Courier inbox/read APIs and the proposed final-mile-offer producer. Defined scoped list/count/detail/read contracts, deduplication, safe Flutter navigation and polling, privacy, and verification gates. Documentation only; no backend or Flutter behavior changed.

- Implemented the protected Courier notification API: scoped list, detail, unread count, signed cursor pagination, safe DTO projection, idempotent mark-read, and private cache headers. Added deterministic after-commit delivery for pickup schedule assignment/revision/cancellation/reminders and final-mile offers, while preserving Seller notification delivery and the Courier dashboard aggregate scaffold. Added Courier and final-mile integration coverage; the focused suites pass 35 tests/708 assertions. Flutter UI remains external and unimplemented here; full PostgreSQL, re-offer/concurrency, and Flutter verification remain release gates. No migration required.

- Revised the Customer Product Reviews and Ratings target spec against the delivered-order and Product foundations. Defined one verified review per delivered Order Item, safe public listing and aggregates, optional images under the shared upload policy, and deferred video/edit/moderation boundaries. Clarified that review persistence/API/UI are not yet implemented and seeded rating values are not verified reviews; documentation only.

- Implemented Customer Product Reviews and Ratings MVP: additive UUID review/image tables, delivered Order Item ownership and one-review uniqueness, transactional published-review aggregates, safe public Product review/photo reads, strict image validation/storage metadata, and Customer Order Detail/Product Detail UI with accessible rating, pagination, upload progress, retry, and empty/error states. Reset catalog seed rating projections so demo values are not presented as verified reviews. Focused review coverage passes 3 tests/29 assertions; existing order/fulfillment coverage passes 19 tests/459 assertions; PostgreSQL applied both pending local migrations successfully; webapp lint, TypeScript, and webpack production build pass. The default Turbopack build remains blocked in this environment by a process-binding permission error, and the full suite still has unrelated pre-existing recently-viewed/filesystem failures.

## 2026-09-22

- Fixed the Courier development mockup pickup map to load its authenticated style from the configured API origin. Legacy ready pickup route manifests now return `pending` with stale GeoJSON and route summaries cleared while Laravel rebuilds them, then return the new ready geometry. Added a pending-to-ready API regression test and updated the pickup contract. Focused route tests pass 3 tests/59 assertions; Courier mockup build and lint pass. Live browser/map-provider verification remains unrun.

- Excluded `maplibre-gl` from the Courier mockup's Vite dependency optimizer after its prebundled module resolved a worker file missing from `.vite/deps`; the installed package contains the worker beside its source module. Courier mockup build and lint pass, and Vite's optimizer no longer lists MapLibre among prebundled dependencies. Live browser map verification remains pending.

- Updated both Courier mockup route maps to show the Logistics hub as an accessible `L` marker and added a restrained glow to route lines and stop markers. Courier mockup lint, TypeScript, and Vite production build pass; live map rendering remains unverified.

- Refined the shared Courier mockup Logistics `L` marker to a circle in both route maps while retaining its glow and accessible label.

## 2026-09-23

- Implemented GitHub issue #97 company-truck linehaul dispatch with issue #98 used only as capacity context: additive string-backed truck/trip/reservation schema, Logistics-scoped fleet CRUD/monitoring and driver capability, deterministic truck-capacity parcel reservation across lanes sharing one next hub, destination approval before outbound departure, atomic manifest custody, visiting-truck receipt, cargo or empty scheduled return without another acceptance, return notifications/Courier trip API, resource-conflict gates, legacy departure bypass prevention, and retained historical receipts plus the unchanged 15-parcel final-mile limit. Added separate Company fleet and Inbound linehaul pages and embedded outbound linehaul in Dispatch using uncodixfy. Added the issue as the canonical checked specification and synchronized fleet, routing, dispatch, registration, notification, domain, requirements, and schema docs. Focused affected API regressions pass 75 tests/1,427 assertions. Logistics lint, TypeScript/Vite production build, and changed-file Pint pass. Full-repository Pint still reports unrelated pre-existing formatting in Admin/platform/policy files; PostgreSQL worker concurrency and physical browser/device/truck handoff remain unverified.

- Fixed the PostgreSQL company-truck migration by adding the `linehaul_trips.parent_trip_id` self-reference after the table primary key exists; the previously pending migration now applies successfully to local PostgreSQL. Updated both Courier fixture seeders so the lead/first generic Couriers and one Courier per Luzon Logistics organization are truck-qualified, including idempotent capability-revision upgrades for existing fixtures. Focused seed/linehaul coverage passes 5 tests/166 assertions; eight qualified Luzon Courier affiliations were confirmed in local PostgreSQL.

- Separated last-mile and outbound company-truck work into `/dispatch` and a new protected `/linehaul-dispatch` page. Linehaul dispatch now lists eligible parcels by physical lane, supports lane filtering, visible/group select-all and individual selection across lanes sharing one immediate destination, and uses Courier-style searchable modals for available trucks and qualified drivers. The API reserves the exact selected Shipment IDs with tenant/destination/lane/capacity revalidation and retains eligible parcels staged under earlier active lane-plan generations. Added Beta sidebar tags for Linehaul dispatch and Inbound linehaul using uncodixfy. Focused company-truck/manifest/multi-hub coverage passes 37 tests/567 assertions; Logistics lint and production build pass. Browser/device visual verification remains pending.

- Implemented inbound company-truck receiving and reconciliation with separate arrival, per-parcel custody, and unloading-closure events. Added trip-scoped idempotent batch receipt, shortage/damage/unexpected discrepancy records, documented damage release, sender discrepancy notifications, late receipt after truck return, `receiving`/`unloading` resource conflicts, individual manifest completion, and compatibility gates for legacy receipt routes. Receive at hub now provides account/organization/hub/trip-isolated offline capture with a production shell cache, authoritative sync counts, and Inbound linehaul reconciliation/return controls. Updated company-truck, routing, sorting, fulfillment, waybill, auth, fleet, notification, domain, requirements, workspace, schema, and verification documentation. SQLite affected regressions pass 57 tests/1,247 assertions; disposable PostgreSQL passes 7 tests/357 assertions including concurrent devices and scan/closure races; Logistics lint and production build plus changed-file Pint pass. Two live Firefox profiles verified offline reload/reconnect, duplicate scans, account isolation, damaged receipt, shortage closure, documented release, mobile dark mode, and return controls. The additive migration passed disposable PostgreSQL migration/rollback and was applied successfully to the local development PostgreSQL database. Physical camera/printed-label and dock/truck verification remain pending.

- Added last-mile COD collection declarations derived from each Order's payable total, a tenant/hub-scoped paginated Logistics delivery-confirmation queue and protected review page, and atomic COD payment marking with Logistics approval. Updated the Courier mockup with rear-camera capture, preview/retake/device-photo fallback, explicit COD acknowledgment, pending-review active tasks, and unsuccessful-attempt retry wording. Added the Logistics feature spec and synchronized Courier, domain, and schema docs. FinalMileFulfillmentTest passes 10 tests/429 assertions; Courier and Logistics lint and production builds pass; changed-file Pint and diff checks pass. PostgreSQL concurrency and connected browser/camera review remain pending.

## 2026-09-24

- Refined the Logistics Delivery confirmations page with uncodixfy: corrected the search input/action proportions, added a compact responsive toolbar, bounded queue, clearer selected state and review hierarchy, width-safe proof preview, and touch-friendly actions. Mobile, tablet/iPad, and current-device widths now stack the queue and review while wide desktop uses a constrained two-column workspace. Logistics lint and production build pass; connected browser/device visual verification remains pending.

- Implemented commission, COD quotation, settlement, and role-isolated profit workspaces: published PSGC zone rates with Logistics acceptance and item-based billable weight; exact per-Shop COD quotes and immutable pricing/commission/cost snapshots; saved-rate pickup eligibility and safe fulfillment holds; balanced finance journals, evidence-based distance allocation, full-COD remittance reconciliation, 14-day daily sandbox payouts, reversing expense corrections, and equal shared-trip cost allocation; plus Admin, Seller, and Logistics Finance dashboards with forecasts, charts, ledger/CSV, payout/remittance views, Order drill-down, and tenant-safe money flow. Added canonical shared specs and synchronized checkout, Product, pickup, fulfillment, linehaul, and reporting specs. The affected suite passes 27 tests/210 assertions, and the broader API suite excluding the unrelated date-frozen `CustomerRecentlyViewedTest` passes 383 tests/4,576 assertions with two skips. All role frontends pass lint, Admin/Seller/Logistics Vite builds pass, the storefront Webpack production build passes, route discovery and PHP syntax pass, and changed finance files pass Pint. PostgreSQL concurrency and live browser/device checks remain release gates; live payout providers remain outside this sandbox release.

- Fixed PostgreSQL creation ordering for the finance journal reversal and expense correction self-references by adding their foreign keys after each table's primary key exists. The finance migration now applies successfully to local PostgreSQL; focused Finance and checkout regressions pass 12 tests/80 assertions, and the migration passes Pint.

- Redesigned the shared Seller, Admin, and Logistics Finance workspace with uncodixfy: added role-aware summary cards, a primary revenue graph combining recorded results with the server-provided 30-day low/base/high projection, a revenue-versus-cost chart, proportional revenue breakdown, compact remittance/payout cards, and a responsive ledger. Removed the separate actual/forecast presentation while retaining accessible chart data and insufficient-history guidance. Focused Finance API coverage passes 4 tests/15 assertions; Pint, all three dashboard lints, and all three production builds pass. Live browser/device visual verification remains pending.
